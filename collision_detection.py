# Pygame Collision Detection Practice, Williams Keyshawn, January 24, 2022, 2:36pm, v0.2 

import pygame, sys, random 
from pygame.locals import * 

# Setup Pygame
pygame. init()
mainClock = pygame.time.clock() 

# Setup the Pygame Window 
WINDOWWIDTH =400 
WINDOWJEIGHT = 400 
windowSurface = pygame.display.set-model((WINDOWWIDTH, WINDOWHEIGHT), 0, 32)
pygame.display.set_ caption('Collision Detection 2022')